#ifdef BUMPY_PLAYABLE
#include "host.h"
/* Skeleton — real bodies land in later tasks. */
/* wait_keypress — spin until a key tick; int8-timing leaf. */
void wait_keypress(void)                            {}
/* fun_75a2_poll_action — input-poll primitive (returns the action byte in AL). */
char fun_75a2_poll_action(u8 arg)                   { (void)arg; return 0; }
#endif
