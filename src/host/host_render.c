#ifdef BUMPY_PLAYABLE
#include "host.h"
/* Skeleton — leaf render wrappers; real bodies land in Tasks 3-4. */
/* (no stubs needed here for Task 1 — the leaf wrappers are not yet called
 * from any reconstructed path that the playable link set exercises.
 * host_fb_init is declared in host.h; its body lands in a later task.) */
#endif
