#ifdef BUMPY_PLAYABLE
#include "host.h"
/* Skeleton — real bodies land in later tasks. */
/* install_interrupt_handler — INT timer handler install. */
void install_interrupt_handler(void)                {}
/* rotate_timing_flags_and_wait — rotate timing flags + block for frame tick. */
void rotate_timing_flags_and_wait(void)             {}
/* run_n_frames — block for N frame ticks driven by the int8 timer ISR. */
void run_n_frames(u8 n)                             { (void)n; }
#endif
