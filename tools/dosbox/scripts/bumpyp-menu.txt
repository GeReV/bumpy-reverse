# BUMPYCAP input script — drive PLAYABLE BUMPYP.EXE through the two reconstructed
# config screens (gfx_driver_init + sound_device_select_screen) into run_main_menu,
# to verify the TITRE.VEC menu raster renders.
#
# Driven with BUMPYCAP_SCAN_INJECT=1 (KEYBOARD_AddBuffer -> real IRQ1/INT9 + BIOS buf):
#   - config_screens.c reads via BIOS INT 16h (host INT9 ISR not yet installed) — F-keys
#   - run_main_menu reads via the host INT9 ISR -> g_key_state_table — FIRE = Enter (0x1c)
# Format: <frame-dec> <scancode-hex> <value>   (1=down, 0=up; '#'=comment)
#
# Screen 1 — gfx_driver_init: F3 (0x3d) -> palette_mode=2 (VGA).  Inject over a window;
# the blocking INT16h grabs the first F3, leftovers fall through to screen 2 (ignored).
60 3d 1
66 3d 0
100 3d 1
106 3d 0
# Screen 2 — sound_device_select_screen: F5 (0x3f) -> sound_device_state=0x8000 (no sound).
200 3f 1
206 3f 0
240 3f 1
246 3f 0
# run_main_menu — FIRE = Enter (0x1c).  Wide holds for the release-debounce.
700 1c 1
760 1c 0
860 1c 1
920 1c 0
