# Instrumentation patches for the pinned dosbox-staging build live here.
# Tracked (the recipe); applied by tools/dosbox/build-staging.sh onto the pinned source.
# None yet — the first build is STOCK, to verify the toolchain before patching.
